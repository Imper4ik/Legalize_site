# Django settings
SECRET_KEY=your-secret-key
DEBUG=False
DATABASE_URL=
RENDER_EXTERNAL_HOSTNAME=

# Email settings (Brevo SMTP)
# Leave EMAIL_HOST_USER and EMAIL_HOST_PASSWORD blank to fall back to console emails locally.
EMAIL_HOST=smtp-relay.brevo.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=
