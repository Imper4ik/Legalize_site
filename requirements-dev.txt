-r requirements.txt
coverage>=7.4.4
pytest==9.1.1
pytest-cov>=5.0.0
pytest-django==4.12.0
ruff==0.15.22
mypy==1.18.2
bandit==1.9.4
pip-audit==2.10.1
django-stubs[compatible-mypy]
# Pillow >=10.3 ships type information; no external Pillow stubs are needed.
# Used only by the generate_stress_data management command (dev/QA load data).
Faker==40.35.0
# Browser for the mobile-layout regression test (test_mobile_overflow.py).
# The test auto-skips when playwright or its chromium binary is unavailable
# (e.g. plain CI); run `playwright install chromium` locally to enable it.
playwright>=1.49
